package main;

import java.util.Scanner;

public class Programmers {
	public static void main(String[] args) {
		// lux < rdx,luy < rdy
		int lux = 0, luy = 0, rdx = 0, rdy = 0;
		// (lux,luy) 로부터 (rdx,rdy) 까지의 거리
		int distance = Math.abs(rdx - lux) + Math.abs(rdy - luy);

		//입력 받아들이기
		Scanner scanner = new Scanner(System.in);
		//1~50개의 스트링 배열로 주어짐
		
		
		//
		
		//최단거리 distance에 대해서 [lux,luy,rdx,rdy] 를 리턴
		int[] answer = {};
//	        return answer;
		scanner.close();
	}

}
